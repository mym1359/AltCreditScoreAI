name: Daily Commit

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Create daily file
        run: |
          echo "Last updated: $(date)" > daily.txt

      - name: Commit changes
        run: |
          git config --global user.name "Auto Commit Bot"
          git config --global user.email "bot@example.com"
          git add -f daily.txt
          git commit -m "Daily commit: $(date)" || echo "No changes to commit"

      - name: Push changes using PAT
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git remote set-url origin https://x-access-token:${GH_PAT}@github.com/mym1359/AltCreditScoreAI.git
          git push origin HEAD